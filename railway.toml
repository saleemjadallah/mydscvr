[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npm run build"

[deploy]
startCommand = "cd backend && npm run db:push && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
